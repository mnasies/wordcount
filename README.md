# wordcount
CLI tool that counts word frequency across one or more text files/directories and prints the top N most common words, sorted by count. Built in Rust to learn ownership, borrowing, Result/?, iterators, and manual CLI arg parsing
